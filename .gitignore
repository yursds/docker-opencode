# User-specific config — contains personal paths and API keys
configs/*.env
!configs/_template.env

# Persistent opencode data — per-project, not shared
data/

# Bash history — per-project per-container, contains personal shell history
.data_history/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
